@using vp_server.Models.ViewModels
@model vp_server.Models.ViewModels.ProductViewsTransactions
<!DOCTYPE html>
<html>
    <body>
    <label for="">Количество в данный момент: @Model.Count</label>
        <form>
        <label>Добавление продукции:</label>
            <input type="number" id="qnt" name="quantity" />
        <input type="button" onclick="addQuantityClicked()" id="btnPost" value="Добавить" />
    </form>
    <script type="text/javascript">
        function addQuantityClicked() {
            var abv = '@Url.Action("_Quantity", "Home")';
            $.ajax({
                url: abv,
                type: 'POST',
                data: { "idProduct": @Model.Product.Id, "quantity": $("#qnt").val() },
                success: function (request) {
                    alert("Количество на складе: " + request)
                },
                error: function () {
                    alert("пиздос")
                }

            });
        }
    </script>
    </body>  
</html>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>


